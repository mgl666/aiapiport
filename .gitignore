# compiled binaries
/aiapiport-bin
/aiapiport-bin.exe
/dist/
/build/
*.egg-info/

# local config with real keys — never commit this
/config.yaml

# Python package artifacts (no longer used, project is Go-only)
/aiapiport/

.env
.claude
.DS_Store